#!/usr/bin/env sh
set -eu

if ! command -v bun >/dev/null 2>&1; then
  echo "senny-bun requires Bun on PATH. Install Bun or use the default senny command." >&2
  exit 127
fi

DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec bun "$DIR/../dist/cli.js" "$@"
